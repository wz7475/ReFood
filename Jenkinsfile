pipeline {
    agent any
    stages {
        stage('test-pipeline-trigger') {
            steps {
                sh 'echo "TEST 3, trying to trigger Jenkins from push"'
            }
        }
    }
}
