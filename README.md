# Step by step instruction of instalation

1. Clone repository
2. Enter cloned repository end execute following commands:
    - docker-compose build
    - docker-compose up
3. The application is available under address 127.0.0.1:8090/
