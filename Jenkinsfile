pipeline {
    agent any
    stages {
        stage('test-pipeline-trigger') {
            steps {
                sh 'echo "TEST 4, fix settings in gitlab integration with jenkins"'
            }
        }
    }
}
